import React, { memo } from "react";
import { use<%= formName %> } from "./index.hooks";

type <%= formName %>Props = {}

export const <%= formName %> = memo(({}: <%= formName %>Props) => {
  const {
    styles,
    classes,
    atomicStyles,
    register,
    onSubmit,
  } = use<%= formName %>();

  return (
    <form onSubmit={onSubmit}></form>
  );
});
