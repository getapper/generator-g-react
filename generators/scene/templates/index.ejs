import React, { memo } from "react";
<% if (useHooks) { %>import { use<%= sceneName %> } from "./index.hooks";

<% } %>type <%= sceneName %>Props = {}

export const <%= sceneName %> = memo(({}: <%= sceneName %>Props) => {
<% if (useHooks) { %>  const {<% if (withStyles) { %>
    styles,
    classes,
    atomicStyles,
  <% } %>} = use<%= sceneName %>();

<% } %>  return (
    <div />
  );
});
