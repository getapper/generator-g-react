import { createSlice } from "@reduxjs/toolkit";
import * as selectors from "./<%= pCsliceName %>.selectors";
import { <%= pCsliceName %>State } from "./<%= pCsliceName %>.interfaces";
import * as extraActions from "../../extra-actions";
<% if (useSagas) { %>import * as sagas from "./<%= pCsliceName %>.sagas";
<% } %>
const initialState: <%= pCsliceName %>State = {};

export const <%= sliceName %>Store = createSlice({
  name: "<%= sliceName %>",
  initialState,
  reducers: {},
});

export { selectors<% if (useSagas) { %>, sagas<% } %> };
